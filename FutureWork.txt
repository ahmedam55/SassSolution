Future Work:
	Remove Dependency on Better completion package 
	Register as package control public package
	add support to @extend directive completion in Sass
	add support to Less
	activate sass processing if only that path is within the current project